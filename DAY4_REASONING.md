# День 4: Разные способы рассуждения AI

## Цель

Сравнить 4 разных подхода к решению одной и той же бизнес-задачи с помощью AI:
1. **Прямой подход** - простой вопрос без инструкций
2. **Пошаговое рассуждение** - явные инструкции для step-by-step решения
3. **AI-сгенерированный промпт** - мета-промптинг (AI создает промпт, затем решает)
4. **Панель экспертов** - несколько виртуальных экспертов с разными точками зрения

## Бизнес-кейс для тестирования

```
Компания получает 500 резюме в месяц. HR-отдел из 3 человек тратит по 20 минут на каждое резюме.

Текущая ситуация:
- Общее время на обработку: 500 × 20 мин = 10,000 минут (166 часов) в месяц
- На 1 HR-специалиста: ~55 часов в месяц только на просмотр резюме
- Проблемы: усталость персонала, возможные пропуски хороших кандидатов, медленный процесс

Цель: Оптимизировать процесс найма, сократив время обработки и улучшив качество отбора кандидатов.
```

## Реализация

### Архитектура

```
ReasoningPrompts.kt
├─ BUSINESS_CASE (фиксированный кейс)
├─ getDirectPrompt()
├─ getStepByStepPrompt()
├─ getAIPromptGenerationRequest()
└─ getExpertsPanelPrompt()

ReasoningComparisonService.kt
├─ runDirectApproach()
├─ runStepByStepApproach()
├─ runAIPromptApproach() (2 API calls)
└─ runExpertsPanelApproach()

ReasoningViewModel.kt
├─ State для каждого подхода (result, loading, error)
├─ runAllApproaches() - запустить все параллельно
└─ enrichWithMetrics() - подсчет wordCount и hasSteps

ReasoningScreen.kt
├─ Бизнес-кейс (всегда видимый)
├─ TabRow с 4 вкладками
└─ ReasoningTabContent для каждого подхода
```

### Метрики рассуждения

В `ResponseMetadata` добавлены поля для анализа:

```kotlin
val wordCount: Int? = null      // Количество слов в ответе
val hasSteps: Boolean? = null   // Есть ли пошаговая структура
```

**Определение пошаговой структуры:**
- Нумерованные списки (1., 2., 3.)
- Явные маркеры "Шаг 1", "Step 1", "Этап 1"
- Markdown заголовки (###)

### Подходы

#### 1. Прямой подход (Direct)

**Промпт:**
```
[Бизнес-кейс]

Как решить эту проблему? Дай конкретные рекомендации.
```

**Особенности:**
- Минимальные инструкции
- AI сам выбирает формат ответа
- Temperature: 0.7

#### 2. Пошаговое рассуждение (Step-by-step)

**Промпт:**
```
[Бизнес-кейс]

ВАЖНО: Реши эту задачу пошагово:
1. Сначала проанализируй проблему
2. Определи ключевые узкие места
3. Предложи варианты решений для каждого узкого места
4. Выбери оптимальное решение
5. Опиши план внедрения

Выполняй каждый шаг последовательно, явно показывая свои рассуждения.
```

**Особенности:**
- Явные шаги в промпте
- Требование показывать рассуждения
- Temperature: 0.7

#### 3. AI-сгенерированный промпт (Meta-prompting)

**Шаг 1 - Генерация промпта:**
```
Ты - эксперт по созданию промптов для AI. Твоя задача: создать ОПТИМАЛЬНЫЙ промпт для решения следующей бизнес-задачи:

[Бизнес-кейс]

Создай промпт, который заставит AI дать максимально эффективное и практичное решение.

Верни ТОЛЬКО текст промпта, без дополнительных объяснений.
```

**Шаг 2 - Использование промпта:**
Сгенерированный промпт используется для решения задачи.

**Особенности:**
- **2 API вызова** (генерация промпта + решение)
- AI оптимизирует промпт под задачу
- Temperature: 0.8 для генерации, 0.7 для решения

#### 4. Панель экспертов (Experts Panel) - Множественные агенты

**Архитектура: 4 отдельных API вызова**

**Вызов 1 - HR-специалист (Мария):**
```
Ты - Мария, HR-специалист с 10-летним опытом в рекрутинге.
Проанализируй ситуацию ТОЛЬКО с точки зрения HR:
1. АНАЛИЗ ПРОБЛЕМЫ (влияние на качество отбора, HR-риски)
2. HR-РЕШЕНИЯ (оптимизация процессов, качество скрининга)
3. РЕКОМЕНДАЦИИ (конкретные шаги, метрики)
```

**Вызов 2 - IT-аналитик (Дмитрий):**
```
Ты - Дмитрий, IT-аналитик, специалист по автоматизации.
Проанализируй ситуацию ТОЛЬКО с точки зрения IT:
1. ТЕХНОЛОГИЧЕСКИЙ АНАЛИЗ (что автоматизировать, какие технологии)
2. IT-РЕШЕНИЯ (ATS, AI/ML, интеграции)
3. ТЕХНИЧЕСКИЕ РЕКОМЕНДАЦИИ (архитектура, инструменты, этапы)
```

**Вызов 3 - Бизнес-консультант (Елена):**
```
Ты - Елена, бизнес-консультант, эксперт по оптимизации процессов.
Проанализируй ситуацию ТОЛЬКО с точки зрения бизнеса:
1. ФИНАНСОВЫЙ АНАЛИЗ (стоимость процесса, скрытые издержки)
2. ROI РАСЧЕТЫ (экономия, срок окупаемости, метрики)
3. БИЗНЕС-РЕКОМЕНДАЦИИ (приоритеты, масштабируемость, KPI)
```

**Вызов 4 - Синтез (Модератор):**
```
Перед тобой мнения трех экспертов. Создай ИТОГОВОЕ РЕШЕНИЕ:
1. СИНТЕЗ МНЕНИЙ (общие точки, дополнения, противоречия)
2. КОМПЛЕКСНОЕ РЕШЕНИЕ (HR + IT + Бизнес)
3. ПЛАН ДЕЙСТВИЙ (шаги, приоритеты, результаты)
```

**Особенности:**
- **4 независимых агента** (3 эксперта + 1 синтез)
- Каждый эксперт - отдельный API вызов с собственной ролью
- Модератор получает все мнения и создает итоговое решение
- Финальный ответ содержит ВСЕ мнения + синтез
- Temperature: 0.6

## UI

### Навигация

Добавлена новая вкладка **"🧠 Рассуждение"** в главную навигацию приложения.

### Экран Reasoning

**Структура:**
1. **Header** - заголовок с кнопками:
   - "Запустить все" - запуск всех 4 подходов параллельно
   - "🔄" - сброс всех результатов

2. **Бизнес-кейс карточка** - всегда видимая, описание задачи

3. **TabRow** - 4 вкладки:
   - Прямой
   - Пошаговый
   - AI-промпт
   - Эксперты

4. **Tab Content** для каждого подхода:
   - Описание подхода
   - Кнопка "▶ Запустить"
   - Индикатор загрузки
   - Метрики результата (Слов, Шаги)
   - Предупреждения (если есть)
   - Содержимое ответа

### Метрики

Отображаются для каждого результата:
- **Слов** - количество слов в ответе
- **Шаги** - "Да" если обнаружена пошаговая структура, иначе "Нет"

## Технические детали

### API вызовы

**Прямой подход (1):**
- 1 API вызов к Yandex GPT
- maxTokens: 8000
- temperature: 0.6

**Пошаговый подход (2):**
- 1 API вызов к Yandex GPT
- maxTokens: 8000
- temperature: 0.6

**AI-сгенерированный промпт (3):**
- 2 API вызова:
  1. Генерация промпта (temperature: 0.8, maxTokens: 2000)
  2. Решение задачи (temperature: 0.6, maxTokens: 8000)

**Панель экспертов (4):**
- **4 API вызова** (каждый эксперт + синтез):
  1. HR-специалист (temperature: 0.6, maxTokens: 8000)
  2. IT-аналитик (temperature: 0.6, maxTokens: 8000)
  3. Бизнес-консультант (temperature: 0.6, maxTokens: 8000)
  4. Модератор-синтез (temperature: 0.6, maxTokens: 8000)

### Обработка ответов

```kotlin
private fun enrichWithMetrics(response: AIResponse): AIResponse {
    val content = response.response.content

    // Подсчет слов
    val wordCount = content.split(Regex("\\s+")).filter { it.isNotBlank() }.size

    // Определение пошаговой структуры
    val hasSteps = detectStepByStepStructure(content)

    return response.copy(...)
}
```

### Определение шагов

```kotlin
private fun detectStepByStepStructure(content: String): Boolean {
    // 1. Нумерованные списки (1., 2., 3., ...)
    val numberedSteps = Regex("""(?:^|\n)\s*\d+\.\s+""").findAll(content).count()
    if (numberedSteps >= 3) return true

    // 2. Ключевые слова шагов
    val stepKeywords = listOf("Шаг", "шаг", "Step", "step", "Этап", "этап")
    val stepMatches = stepKeywords.sumOf { keyword ->
        Regex("$keyword\\s*\\d+").findAll(content).count()
    }
    if (stepMatches >= 3) return true

    // 3. Markdown заголовки
    val headers = Regex("""(?:^|\n)#{1,3}\s+""").findAll(content).count()
    if (headers >= 3) return true

    return false
}
```

## Использование

### Запуск отдельного подхода

1. Перейти на вкладку "🧠 Рассуждение"
2. Выбрать нужный подход (вкладка)
3. Нажать "▶ Запустить"
4. Дождаться результата
5. Просмотреть метрики и содержимое

### Запуск всех подходов

1. Перейти на вкладку "🧠 Рассуждение"
2. Нажать "Запустить все" в header
3. Переключаться между вкладками для просмотра результатов
4. Сравнить метрики (wordCount, hasSteps)

### Сброс результатов

Нажать "🔄" в header для очистки всех результатов и повторного тестирования.

## Ожидаемые результаты

### Гипотезы для проверки

1. **Прямой подход:**
   - Самый короткий ответ (меньше слов)
   - Может не иметь явной пошаговой структуры
   - 1 API вызов - быстрый

2. **Пошаговый подход:**
   - Длинный ответ (больше слов)
   - hasSteps = true
   - Детальное рассуждение по шагам
   - 1 API вызов

3. **AI-промпт:**
   - Наиболее оптимизированный ответ
   - Может быть самым практичным
   - 2 API вызова - медленнее
   - Зависит от качества сгенерированного промпта

4. **Панель экспертов:**
   - Самый длинный ответ (3 эксперта + синтез)
   - hasSteps = true (структура по экспертам)
   - Множество перспектив (HR + IT + Бизнес)
   - **4 API вызова - самый медленный**
   - Каждый эксперт - независимый агент

### Метрики для сравнения

- **Длина ответа** (wordCount) - полнота решения
- **Структурированность** (hasSteps) - ясность изложения
- **Практичность** - субъективная оценка пользователя
- **Время выполнения** - AI-промпт требует 2 вызова

## Выводы

Эта реализация позволяет:
- ✅ Сравнивать разные стратегии промптинга на одной задаче
- ✅ Измерять объективные метрики (слова, структура)
- ✅ Выбирать оптимальный подход для конкретных задач
- ✅ Демонстрировать влияние промпта на качество ответа

**Основной урок:** Способ формулировки задачи сильно влияет на результат. Даже простое добавление пошаговых инструкций может кардинально изменить структуру и полноту ответа.

## Файлы проекта

### Shared модуль
- `shared/src/commonMain/kotlin/com/example/ai_window/service/ReasoningPrompts.kt` - промпты для всех подходов
- `shared/src/commonMain/kotlin/com/example/ai_window/service/ReasoningComparisonService.kt` - сервис для выполнения подходов
- `shared/src/commonMain/kotlin/com/example/ai_window/model/YandexGptModels.kt` - расширена ResponseMetadata

### ComposeApp модуль
- `composeApp/src/commonMain/kotlin/com/example/ai_window/ReasoningViewModel.kt` - управление состоянием
- `composeApp/src/commonMain/kotlin/com/example/ai_window/ReasoningScreen.kt` - UI для сравнения
- `composeApp/src/commonMain/kotlin/com/example/ai_window/App.kt` - добавлена вкладка Reasoning

## Дальнейшие улучшения

- [ ] Добавить сохранение результатов для сравнения между сессиями
- [ ] Показывать время выполнения каждого подхода
- [ ] Добавить возможность изменить бизнес-кейс
- [ ] Экспорт результатов сравнения в markdown
- [ ] Графическое сравнение метрик (диаграммы)
