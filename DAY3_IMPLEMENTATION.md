# Day 3 - Requirements Gathering Agent

## Цель
Реализовать AI-ассистента, который самостоятельно собирает требования к проекту через естественный диалог и автоматически генерирует техническое задание.

## Реализованная функциональность

### 1. Системный промпт для сбора требований
**Файл:** `shared/src/commonMain/kotlin/com/example/ai_window/service/RequirementsPrompt.kt`

Промпт инструктирует модель:
- Вести естественный диалог (не анкетный опрос)
- Собрать 6 обязательных разделов требований
- **Самостоятельно** определить момент завершения
- **Автоматически** сгенерировать финальное ТЗ без запроса разрешения

#### 6 обязательных разделов:
1. `project_overview` - название, описание, цели
2. `target_audience` - аудитория, масштаб
3. `features` - функции с приоритетами (must/should/nice to have)
4. `technical_requirements` - платформы, технологии, интеграции
5. `non_functional` - производительность, безопасность, SLA
6. `timeline_budget` - сроки, ограничения

#### Условия автоматического завершения:
Модель **самостоятельно** останавливает диалог и генерирует ТЗ когда:
- Все 6 разделов имеют минимальную информацию
- ИЛИ задано 15 вопросов (лимит)
- ИЛИ пользователь явно просит закончить

### 2. Расширенные метаданные для отслеживания прогресса
**Файл:** `shared/src/commonMain/kotlin/com/example/ai_window/model/YandexGptModels.kt`

Добавлены поля в `ResponseMetadata`:
```kotlin
data class ResponseMetadata(
    val confidence: Double = 0.5,
    val category: String = "general",
    // Day 3: Progress tracking
    val sections_completed: List<String>? = null,  // Собранные разделы
    val questions_asked: Int? = null,              // Счетчик вопросов
    val is_complete: Boolean? = null               // true когда ТЗ готово
)
```

Новые категории:
- `REQUIREMENTS_GATHERING` - процесс сбора требований
- `REQUIREMENTS_COMPLETE` - финальное ТЗ готово

### 3. Сервис для режима сбора требований
**Файл:** `shared/src/commonMain/kotlin/com/example/ai_window/service/RequirementsGatheringService.kt`

Специализированный сервис с:
- Методом `startSession()` - инициализация диалога с системным промптом
- Методом `sendMessage()` - обработка сообщений пользователя
- Повышенными параметрами:
  - `temperature = 0.7` - для более естественного диалога
  - `maxTokens = 8000` - для длинного финального документа

### 4. ViewModel для управления состоянием
**Файл:** `composeApp/src/commonMain/kotlin/com/example/ai_window/PlanningViewModel.kt`

Управляет:
- Историей диалога
- Прогрессом сбора (какие разделы собраны)
- Счетчиком заданных вопросов
- Флагом завершения (is_complete)

### 5. UI с индикатором прогресса
**Файл:** `composeApp/src/commonMain/kotlin/com/example/ai_window/PlanningScreen.kt`

Включает:
- **Прогресс-бар**: визуализация `X/6` собранных разделов
- **Чипы разделов**: ✅ для собранных, ⏳ для ожидающих
- **Счетчик вопросов**: отображение в заголовке
- **Блокировка ввода**: после завершения сбора
- **Кнопка сброса**: 🔄 для начала нового проекта

### 6. Навигация между режимами
**Файл:** `composeApp/src/commonMain/kotlin/com/example/ai_window/App.kt`

Добавлены:
- Enum `Screen` с двумя режимами: `Chat` (💬) и `Planning` (📋)
- Навигационная панель с переключением
- Раздельные ViewModel для каждого режима

## Архитектура решения

```
┌─────────────────────────────────────────────┐
│          App.kt (Navigation)                │
│  ┌─────────────┐      ┌──────────────────┐ │
│  │  ChatScreen │      │  PlanningScreen  │ │
│  │  (Day 1-2)  │      │    (Day 3)       │ │
│  └─────────────┘      └──────────────────┘ │
└─────────────────────────────────────────────┘
           │                      │
           ▼                      ▼
  ┌─────────────────┐   ┌────────────────────────┐
  │ ChatViewModel   │   │  PlanningViewModel     │
  │                 │   │  - Progress tracking   │
  │                 │   │  - Auto-completion     │
  └─────────────────┘   └────────────────────────┘
           │                      │
           ▼                      ▼
  ┌─────────────────┐   ┌────────────────────────────┐
  │ YandexGptService│   │ RequirementsGatheringService│
  │  (General)      │   │  - System prompt injection  │
  │                 │   │  - Higher temperature (0.7) │
  │                 │   │  - Larger context (8000)    │
  └─────────────────┘   └────────────────────────────┘
                                 │
                                 ▼
                      ┌─────────────────────┐
                      │ RequirementsPrompt  │
                      │ - 6 sections        │
                      │ - Auto-completion   │
                      │ - Format examples   │
                      └─────────────────────┘
```

## Формат ответов модели

### Во время сбора требований:
```json
{
  "response": {
    "title": "Сбор требований",
    "content": "Какие основные функции должно иметь приложение?",
    "metadata": {
      "confidence": 0.8,
      "category": "requirements_gathering",
      "sections_completed": ["project_overview"],
      "questions_asked": 3,
      "is_complete": false
    }
  }
}
```

### Финальное техническое задание:
```json
{
  "response": {
    "title": "📋 ТЕХНИЧЕСКОЕ ЗАДАНИЕ",
    "content": "📋 ТРЕБОВАНИЯ СОБРАНЫ. ГЕНЕРИРУЮ ТЕХНИЧЕСКОЕ ЗАДАНИЕ...\n\n# ТЕХНИЧЕСКОЕ ЗАДАНИЕ\n\n## 1. Обзор проекта\n...",
    "metadata": {
      "confidence": 0.95,
      "category": "requirements_complete",
      "sections_completed": [
        "project_overview",
        "target_audience",
        "features",
        "technical_requirements",
        "non_functional",
        "timeline_budget"
      ],
      "questions_asked": 12,
      "is_complete": true
    }
  }
}
```

## Ключевые особенности реализации

### 1. Автономность модели
Модель **НЕ СПРАШИВАЕТ** разрешения на завершение:
- ❌ "Достаточно ли информации?"
- ❌ "Могу ли я сгенерировать ТЗ?"
- ✅ Просто генерирует ТЗ когда условия выполнены

### 2. Естественный диалог
Модель ведет беседу, а не опрос:
- ❌ "Вопрос 1: Название проекта?"
- ❌ "Вопрос 2: Целевая аудитория?"
- ✅ "Расскажите подробнее, что должно делать приложение?"
- ✅ "Интересно! А какой масштаб планируете?"

### 3. Умные дефолты
Если пользователь не указывает детали, модель предлагает разумные значения:
- "Предположу, что это веб-приложение, если не указано иное"
- "Бюджет и сроки оставлю открытыми для обсуждения"

### 4. Ограничения
- Максимум 15 вопросов (защита от бесконечного диалога)
- Минимум информации для каждого раздела
- Автоматическая генерация даже при неполных данных

## Пример использования

### 1. Пользователь открывает вкладку "📋 ТЗ"
Система автоматически стартует сессию и AI приветствует:
```
"Привет! Я помогу собрать требования для твоего проекта.
Расскажи, что ты хочешь создать?"
```

### 2. Диалог (пример):
```
User: "Хочу сделать мобильное приложение для трекинга задач"
AI: "Отлично! Для кого это приложение? Маленькая команда или крупная компания?"

User: "Для команды из 10-20 человек"
AI: "Понятно. Какие ключевые функции должны быть обязательно?"

User: "Создание задач, назначение исполнителей, дедлайны"
AI: "Хорошо. А какие платформы нужны? iOS, Android, или обе?"

[... диалог продолжается ...]
```

### 3. Автоматическое завершение:
После сбора всех 6 разделов AI автоматически генерирует:
```
📋 ТРЕБОВАНИЯ СОБРАНЫ. ГЕНЕРИРУЮ ТЕХНИЧЕСКОЕ ЗАДАНИЕ...

# ТЕХНИЧЕСКОЕ ЗАДАНИЕ

## 1. Обзор проекта
**Название:** TaskFlow
**Описание:** Мобильное приложение для управления задачами в небольших командах
**Цели:** Упростить распределение задач и контроль дедлайнов

## 2. Целевая аудитория
**Пользователи:** Команды из 10-20 человек
**Масштаб:** До 50 активных пользователей

[... полное ТЗ ...]
```

## Тестирование

### Запуск приложения:
```bash
./gradlew :composeApp:run
```

### Проверьте:
1. ✅ Переключение между вкладками "💬 Чат" и "📋 ТЗ"
2. ✅ Автостарт сессии при открытии "📋 ТЗ"
3. ✅ Прогресс-бар обновляется по мере сбора разделов
4. ✅ Чипы разделов меняют статус ⏳ → ✅
5. ✅ Счетчик вопросов увеличивается
6. ✅ AI самостоятельно генерирует ТЗ при завершении
7. ✅ Поле ввода блокируется после генерации ТЗ
8. ✅ Кнопка 🔄 сбрасывает сессию

### Тестовый сценарий:
```
1. Откройте вкладку "📋 ТЗ"
2. Введите: "Хочу создать приложение"
3. Отвечайте на вопросы AI
4. Наблюдайте за прогрессом (разделы, вопросы)
5. Дождитесь автоматической генерации ТЗ
6. Убедитесь, что ввод заблокирован
7. Нажмите 🔄 для нового проекта
```

## Логи для отладки

Сервис выводит подробные логи:
```
📤 Starting requirements gathering session...
📥 RAW INITIAL RESPONSE: [JSON]
✅ Parse success: Привет! Я помогу собрать...
📤 Sending message to requirements gathering API...
📥 RAW RESPONSE: [JSON]
✅ Parse success
   Category: requirements_gathering
   Sections: [project_overview]
   Questions: 1
   Complete: false
```

## Соответствие требованиям Day 3

| Требование | Статус | Реализация |
|-----------|--------|------------|
| Модель собирает 6 разделов | ✅ | RequirementsPrompt определяет структуру |
| Самостоятельное завершение | ✅ | Промпт инструктирует генерировать ТЗ автоматически |
| Естественный диалог | ✅ | Temperature 0.7, инструкции против анкетного стиля |
| Ограничение на вопросы (≤15) | ✅ | Отслеживание через metadata.questions_asked |
| Финальный документ ТЗ | ✅ | Шаблон в промпте, category=requirements_complete |
| UI с индикатором прогресса | ✅ | PlanningScreen с прогресс-баром и чипами |

## Особенности Yandex GPT API

### Отсутствие system role
Yandex GPT не поддерживает роль "system", поэтому:
- Системный промпт передается как первое сообщение пользователя
- Формат инструкций повторяется в начале каждого запроса

### Параметры для лучших результатов:
```kotlin
CompletionOptions(
    temperature = 0.7,   // Выше для креативного диалога
    maxTokens = 8000     // Больше для длинного ТЗ
)
```

## Возможные улучшения

### 1. Персистентность
Сохранение сессий в локальное хранилище:
```kotlin
// Сохранить сессию
localStorage.saveSession(messages, sectionsCompleted)

// Восстановить при перезапуске
val savedSession = localStorage.loadSession()
```

### 2. Экспорт ТЗ
Кнопка для экспорта в PDF/Markdown:
```kotlin
fun exportSpecification(specification: String) {
    // Markdown file
    File("specification.md").writeText(specification)

    // Or PDF
    generatePdf(specification, "specification.pdf")
}
```

### 3. Редактирование секций
Возможность вернуться и уточнить конкретный раздел:
```kotlin
fun editSection(sectionId: String) {
    viewModel.requestSectionUpdate(sectionId)
}
```

### 4. Шаблоны проектов
Быстрый старт с пресетами:
```kotlin
enum class ProjectTemplate {
    MOBILE_APP,
    WEB_SERVICE,
    DESKTOP_APP
}

fun startWithTemplate(template: ProjectTemplate) {
    val prefilledData = getTemplateData(template)
    viewModel.startSessionWithData(prefilledData)
}
```

## Итоги Day 3

### Что реализовано:
✅ Системный промпт для автономного сбора требований
✅ Отслеживание прогресса через метаданные
✅ Автоматическое определение момента завершения
✅ Генерация структурированного ТЗ
✅ UI с визуализацией прогресса
✅ Навигация между режимами Chat и Planning

### Ключевые достижения:
- **Автономность**: Модель сама решает, когда завершить
- **Естественность**: Диалог, а не опросник
- **Структурированность**: Гарантированные 6 разделов
- **UX**: Визуальный прогресс и блокировка после завершения

### Следующие шаги:
1. Тестирование с реальными пользователями
2. Сбор обратной связи о качестве генерируемых ТЗ
3. Доработка промпта на основе результатов
4. Добавление экспорта и персистентности

---

**Day 3 completed! 🎉**
